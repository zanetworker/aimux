# Claude Reviewer agent Deployment.
# replicas: 0 — scale-to-zero. spawn_agent MCP tool scales up on demand.
# Results stored in Redis — no git workflow, no workspace volume.
# Same image as coder — role driven by ROLE + ALLOWED_TOOLS env vars.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-claude-reviewer
  namespace: agents
  labels:
    app: agent-claude-reviewer
    team-component: agent
    provider: claude
    role: reviewer
    app.kubernetes.io/part-of: k8s-agents
spec:
  replicas: 0
  selector:
    matchLabels:
      app: agent-claude-reviewer
  template:
    metadata:
      labels:
        app: agent-claude-reviewer
        team-component: agent
        provider: claude
        role: reviewer
        app.kubernetes.io/part-of: k8s-agents
    spec:
      serviceAccountName: mcp-server
      containers:
        - name: agent
          image: quay.io/azaalouk/agent-claude:latest
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis-secret
                  key: password
            - name: REDIS_URL
              value: "redis://:$(REDIS_PASSWORD)@redis:6379"
            - name: TEAM_ID
              value: "my-team"
            - name: AGENT_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: PROVIDER
              value: "claude"
            - name: ROLE
              value: "reviewer"
            - name: MODEL
              value: "claude-sonnet-4-6"
            - name: ALLOWED_TOOLS
              value: "Read,Grep,Glob"
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: llm-keys
                  key: anthropic
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
