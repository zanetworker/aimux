version: 2
builds:
  - main: ./cmd/agentmux
    binary: agentmux
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64

archives:
  - formats: ['tar.gz']

checksum:
  name_template: "checksums.txt"
